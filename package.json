{
  "name": "ai-meeting-notes",
  "private": true,
  "scripts": {
    "doctor": "node scripts/doctor.mjs",
    "test:doctor": "node --test scripts/doctor-env.test.mjs",
    "predev": "node scripts/doctor.mjs",
    "dev": "sh scripts/dev.sh",
    "dev:web": "pnpm --filter @ai-meeting-notes/web dev",
    "dev:api": "cd services/api && .venv/bin/uvicorn main:app --reload --port ${API_PORT:-8000}",
    "build": "pnpm --filter @ai-meeting-notes/web build",
    "typecheck": "pnpm --filter @ai-meeting-notes/web typecheck",
    "lint": "pnpm --filter @ai-meeting-notes/web lint",
    "lint:api": "cd services/api && .venv/bin/ruff check .",
    "test:api": "cd services/api && .venv/bin/python -m pytest",
    "check:structure": "cd services/api && .venv/bin/python -m pytest tests/test_structure.py -v",
    "test:e2e": "pnpm --filter @ai-meeting-notes/web exec playwright test"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "engines": {
    "node": ">=20.9.0",
    "pnpm": ">=9"
  }
}
